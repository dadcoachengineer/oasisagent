# OasisAgent Environment Variables
# Copy this file to .env and fill in your values.
# All variables referenced as ${VAR} in config.yaml are listed here.

# --- Home Assistant ---
# Long-lived access token from HA (Profile → Security → Long-Lived Access Tokens)
HA_TOKEN=your_ha_long_lived_access_token

# --- MQTT Broker (EMQX, Mosquitto, etc.) ---
MQTT_USER=oasisagent
MQTT_PASS=your_mqtt_password

# --- LLM: T1 Triage (local SLM) ---
# API key for local LLM endpoint (Ollama typically doesn't need one)
TRIAGE_LLM_API_KEY=not-needed

# --- LLM: T2 Reasoning (cloud model) ---
# API key for your cloud LLM provider (Anthropic, OpenAI, OpenRouter, etc.)
REASONING_LLM_API_KEY=your_llm_api_key

# --- InfluxDB v2 ---
# API token with write access to your OasisAgent bucket
INFLUXDB_TOKEN=your_influxdb_token

# --- Proxmox (Phase 3, not needed yet) ---
# PROXMOX_USER=root@pam
# PROXMOX_TOKEN_NAME=oasisagent
# PROXMOX_TOKEN_VALUE=your_proxmox_api_token
